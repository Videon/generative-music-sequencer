using UnityEngine;

public class GMSBar : MonoBehaviour
{
    public enum Root { C, D, E, F, G, A, B }
    public enum Scale { }
    public bool[] enabledNotes = new bool[128];

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
